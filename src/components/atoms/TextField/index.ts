import TextField from "./TextField";
import {
  containerVariants,
  inputVariants,
  labelVariants,
  textFieldVariants,
} from "./TextField.styles";
import type { TextFieldBaseProps, TextFieldProps } from "./TextField.types";

export type { TextFieldBaseProps, TextFieldProps };

export { containerVariants, textFieldVariants, labelVariants, inputVariants };

export default TextField;
