import InteractionState from "./InteractionState";
import { focusVariants, interactionStateVariants } from "./InteractionState.style";

export { interactionStateVariants, focusVariants };

export default InteractionState;
